# Changesets

Add a changeset for every user-visible change after the first public releases. The initial `@effect-vfs/core` and
`@effect-vfs/memory` releases start at `0.0.1`, so the repository and release setup itself has no changeset.
