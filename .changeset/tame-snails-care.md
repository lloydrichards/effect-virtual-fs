---
"@effect-vfs/core": minor
---

Add a provider-neutral live image boundary and `LiveImageStore` service. Applications can supply a scoped storage Layer; core stages each mutation, validates recovered images, and stops the volume after an uncertain commit.
