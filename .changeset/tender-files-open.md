---
"@effect-vfs/core": minor
---

`openChildReference` can set a new regular file's initial size in the same atomic operation that creates and opens it. An optional expected child rejects replacement races before changing the target.
