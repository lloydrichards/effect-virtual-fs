# Caller overview

Caller identity brands and schemas: credentials, root caller options, and
the settings and results of the operations a caller performs.

Added in v0.6.0

---

## models

### DirectoryChange (type alias)

A directory's revision before and after a change.

**Signature**

```ts
export type DirectoryChange = { readonly before: bigint; readonly after: bigint }
```

Added in v0.6.0

### Identity (type alias)

A caller's credentials.

**Signature**

```ts
export type Identity = {
  readonly uid: number
  readonly gid: number
  readonly groups: readonly number[]
  readonly privileged: boolean
}
```

Added in v0.6.0

### MkdirOptions (type alias)

Options of `mkdir`.

**Signature**

```ts
export type MkdirOptions = {
  readonly mode?: number
  readonly exactMode?: boolean
  readonly times?: {
    readonly access:
      | { readonly kind: "now" }
      | { readonly kind: "omit" }
      | { readonly kind: "value"; readonly nanoseconds: bigint }
    readonly modification:
      | { readonly kind: "now" }
      | { readonly kind: "omit" }
      | { readonly kind: "value"; readonly nanoseconds: bigint }
  }
}
```

Added in v0.6.0

### OpenEntryOptions (type alias)

Options of `open` on an entry.

**Signature**

```ts
export type OpenEntryOptions = {
  readonly access: "read" | "write" | "readWrite"
  readonly mode?: number
  readonly create?: "never" | "ifMissing" | "exclusive"
  readonly append?: boolean
  readonly truncate?: boolean
  readonly exactMode?: boolean
  readonly times?: {
    readonly access:
      | { readonly kind: "now" }
      | { readonly kind: "omit" }
      | { readonly kind: "value"; readonly nanoseconds: bigint }
    readonly modification:
      | { readonly kind: "now" }
      | { readonly kind: "omit" }
      | { readonly kind: "value"; readonly nanoseconds: bigint }
  }
  readonly followFinalSymlink?: boolean
  readonly initialSize?: bigint
  readonly owner?: { readonly uid?: number; readonly gid?: number }
  readonly expected?: ObjectReference | null
  readonly expectedChild?: {
    readonly reference: ObjectReference
    readonly revision: bigint
    readonly atimeNs: bigint
    readonly mtimeNs: bigint
  } | null
}
```

Added in v0.6.0

### OpenOptions (type alias)

Options of `open` on a target.

**Signature**

```ts
export type OpenOptions = {
  readonly access: "read" | "write" | "readWrite"
  readonly mode?: number
  readonly create?: "never" | "ifMissing" | "exclusive"
  readonly append?: boolean
  readonly truncate?: boolean
}
```

Added in v0.6.0

### RenameReferenceResult (type alias)

The result of `rename`.

**Signature**

```ts
export type RenameReferenceResult =
  | {
      readonly _tag: "SameDirectory"
      readonly directory: { readonly before: bigint; readonly after: bigint }
    }
  | {
      readonly _tag: "DifferentDirectories"
      readonly sourceDirectory: { readonly before: bigint; readonly after: bigint }
      readonly destinationDirectory: { readonly before: bigint; readonly after: bigint }
    }
```

Added in v0.6.0

### RootCallerOptions (type alias)

Options of a root caller.

**Signature**

```ts
export type RootCallerOptions = {
  readonly identity?: {
    readonly uid: number
    readonly gid: number
    readonly groups: readonly number[]
    readonly privileged: boolean
  }
  readonly umask?: number
}
```

Added in v0.6.0

### SymlinkOptions (type alias)

Options of `symlink`.

**Signature**

```ts
export type SymlinkOptions = {
  readonly times?: {
    readonly access:
      | { readonly kind: "now" }
      | { readonly kind: "omit" }
      | { readonly kind: "value"; readonly nanoseconds: bigint }
    readonly modification:
      | { readonly kind: "now" }
      | { readonly kind: "omit" }
      | { readonly kind: "value"; readonly nanoseconds: bigint }
  }
}
```

Added in v0.6.0

### WriteFileOptions (type alias)

Options of `writeFile`.

**Signature**

```ts
export type WriteFileOptions = {
  readonly access: "read" | "write" | "readWrite"
  readonly mode?: number
  readonly create?: "never" | "ifMissing" | "exclusive"
  readonly append?: boolean
  readonly truncate?: boolean
  readonly followFinalSymlink?: boolean
  readonly replaceFinalSymlink?: boolean
  readonly finalMode?: number
}
```

Added in v0.6.0

## schemas

### DirectoryChange

Schema for a directory's revision before and after a change.

**Signature**

```ts
export declare const DirectoryChange: Schema.Struct<{ readonly before: Schema.BigInt; readonly after: Schema.BigInt }>
```

Added in v0.6.0

### Identity

Schema for a caller's credentials. `privileged` is explicit; uid zero alone
grants nothing.

**Signature**

```ts
export declare const Identity: Schema.Struct<{
  readonly uid: Schema.Natural
  readonly gid: Schema.Natural
  readonly groups: Schema.$Array<Schema.Natural>
  readonly privileged: Schema.Boolean
}>
```

Added in v0.6.0

### MkdirOptions

Schema for the options of `mkdir`. `exactMode` skips the caller's umask for
an explicit creation mode; permission policy still applies.

**Signature**

```ts
export declare const MkdirOptions: Schema.Struct<{
  readonly mode: Schema.optionalKey<Schema.Natural>
  readonly exactMode: Schema.optionalKey<Schema.Boolean>
  readonly times: Schema.optionalKey<
    Schema.Struct<{
      readonly access: Schema.Union<
        readonly [
          Schema.Struct<{ readonly kind: Schema.Literal<"now"> }>,
          Schema.Struct<{ readonly kind: Schema.Literal<"omit"> }>,
          Schema.Struct<{ readonly kind: Schema.Literal<"value">; readonly nanoseconds: Schema.BigInt }>
        ]
      >
      readonly modification: Schema.Union<
        readonly [
          Schema.Struct<{ readonly kind: Schema.Literal<"now"> }>,
          Schema.Struct<{ readonly kind: Schema.Literal<"omit"> }>,
          Schema.Struct<{ readonly kind: Schema.Literal<"value">; readonly nanoseconds: Schema.BigInt }>
        ]
      >
    }>
  >
}>
```

Added in v0.6.0

### OpenEntryOptions

Schema for the options of `open` on an entry, a lookup-or-create-and-open in
one gate hold. `expected` pins the child the caller last observed under the
name (or `null` for none) and fails as `VolumeBusy` when another has
appeared; `expectedChild` pins its revision and times as well and fails as
`StaleReference` when they moved.

**Signature**

```ts
export declare const OpenEntryOptions: Schema.Struct<{
  readonly followFinalSymlink: Schema.optionalKey<Schema.Boolean>
  readonly times: Schema.optionalKey<
    Schema.Struct<{
      readonly access: Schema.Union<
        readonly [
          Schema.Struct<{ readonly kind: Schema.Literal<"now"> }>,
          Schema.Struct<{ readonly kind: Schema.Literal<"omit"> }>,
          Schema.Struct<{ readonly kind: Schema.Literal<"value">; readonly nanoseconds: Schema.BigInt }>
        ]
      >
      readonly modification: Schema.Union<
        readonly [
          Schema.Struct<{ readonly kind: Schema.Literal<"now"> }>,
          Schema.Struct<{ readonly kind: Schema.Literal<"omit"> }>,
          Schema.Struct<{ readonly kind: Schema.Literal<"value">; readonly nanoseconds: Schema.BigInt }>
        ]
      >
    }>
  >
  readonly initialSize: Schema.optionalKey<Schema.BigInt>
  readonly exactMode: Schema.optionalKey<Schema.Boolean>
  readonly owner: Schema.optionalKey<
    Schema.Struct<{
      readonly uid: Schema.optionalKey<Schema.Natural>
      readonly gid: Schema.optionalKey<Schema.Natural>
    }>
  >
  readonly expected: Schema.optionalKey<Schema.NullOr<Schema.declare<ObjectReference, ObjectReference>>>
  readonly expectedChild: Schema.optionalKey<
    Schema.NullOr<
      Schema.Struct<{
        readonly reference: Schema.declare<ObjectReference, ObjectReference>
        readonly revision: Schema.BigInt
        readonly atimeNs: Schema.BigInt
        readonly mtimeNs: Schema.BigInt
      }>
    >
  >
  readonly access: Schema.Literals<readonly ["read", "write", "readWrite"]>
  readonly create: Schema.optionalKey<Schema.Literals<readonly ["never", "ifMissing", "exclusive"]>>
  readonly mode: Schema.optionalKey<Schema.Natural>
  readonly append: Schema.optionalKey<Schema.Boolean>
  readonly truncate: Schema.optionalKey<Schema.Boolean>
}>
```

Added in v0.6.0

### OpenOptions

Schema for the options of `open` on a target: the access mode, whether a
missing file is created, the creation mode, and append and truncate flags.

**Signature**

```ts
export declare const OpenOptions: Schema.Struct<{
  readonly access: Schema.Literals<readonly ["read", "write", "readWrite"]>
  readonly create: Schema.optionalKey<Schema.Literals<readonly ["never", "ifMissing", "exclusive"]>>
  readonly mode: Schema.optionalKey<Schema.Natural>
  readonly append: Schema.optionalKey<Schema.Boolean>
  readonly truncate: Schema.optionalKey<Schema.Boolean>
}>
```

Added in v0.6.0

### RenameReferenceResult

Schema for the result of `rename`.

**Signature**

```ts
export declare const RenameReferenceResult: Schema.TaggedUnion<{
  readonly SameDirectory: Schema.TaggedStruct<
    "SameDirectory",
    { readonly directory: Schema.Struct<{ readonly before: Schema.BigInt; readonly after: Schema.BigInt }> }
  >
  readonly DifferentDirectories: Schema.TaggedStruct<
    "DifferentDirectories",
    {
      readonly sourceDirectory: Schema.Struct<{ readonly before: Schema.BigInt; readonly after: Schema.BigInt }>
      readonly destinationDirectory: Schema.Struct<{ readonly before: Schema.BigInt; readonly after: Schema.BigInt }>
    }
  >
}>
```

Added in v0.6.0

### RootCallerOptions

Schema for the options of a root caller.

**Signature**

```ts
export declare const RootCallerOptions: Schema.Struct<{
  readonly identity: Schema.optionalKey<
    Schema.Struct<{
      readonly uid: Schema.Natural
      readonly gid: Schema.Natural
      readonly groups: Schema.$Array<Schema.Natural>
      readonly privileged: Schema.Boolean
    }>
  >
  readonly umask: Schema.optionalKey<Schema.Natural>
}>
```

Added in v0.6.0

### SymlinkOptions

Schema for the options of `symlink`.

**Signature**

```ts
export declare const SymlinkOptions: Schema.Struct<{
  readonly times: Schema.optionalKey<
    Schema.Struct<{
      readonly access: Schema.Union<
        readonly [
          Schema.Struct<{ readonly kind: Schema.Literal<"now"> }>,
          Schema.Struct<{ readonly kind: Schema.Literal<"omit"> }>,
          Schema.Struct<{ readonly kind: Schema.Literal<"value">; readonly nanoseconds: Schema.BigInt }>
        ]
      >
      readonly modification: Schema.Union<
        readonly [
          Schema.Struct<{ readonly kind: Schema.Literal<"now"> }>,
          Schema.Struct<{ readonly kind: Schema.Literal<"omit"> }>,
          Schema.Struct<{ readonly kind: Schema.Literal<"value">; readonly nanoseconds: Schema.BigInt }>
        ]
      >
    }>
  >
}>
```

Added in v0.6.0

### WriteFileOptions

Schema for the options of `writeFile`: the open options plus whether a
final symbolic link is replaced or followed and the mode the file ends with.

**Signature**

```ts
export declare const WriteFileOptions: Schema.Struct<{
  readonly followFinalSymlink: Schema.optionalKey<Schema.Boolean>
  readonly replaceFinalSymlink: Schema.optionalKey<Schema.Boolean>
  readonly finalMode: Schema.optionalKey<Schema.Natural>
  readonly access: Schema.Literals<readonly ["read", "write", "readWrite"]>
  readonly create: Schema.optionalKey<Schema.Literals<readonly ["never", "ifMissing", "exclusive"]>>
  readonly mode: Schema.optionalKey<Schema.Natural>
  readonly append: Schema.optionalKey<Schema.Boolean>
  readonly truncate: Schema.optionalKey<Schema.Boolean>
}>
```

Added in v0.6.0

## type IDs

### CallerId

Brand key that marks a caller.

**Signature**

```ts
export declare const CallerId: typeof CallerId
```

Added in v0.6.0

### CallerId (type alias)

Brand key that marks a caller.

**Signature**

```ts
export type CallerId = typeof CallerId
```

Added in v0.6.0

### ObjectReferenceId

Brand key that marks an object reference.

**Signature**

```ts
export declare const ObjectReferenceId: typeof ObjectReferenceId
```

Added in v0.6.0

### ObjectReferenceId (type alias)

Brand key that marks an object reference.

**Signature**

```ts
export type ObjectReferenceId = typeof ObjectReferenceId
```

Added in v0.6.0